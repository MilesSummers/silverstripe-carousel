$(document).ready(function(){
	$('.bxslider').bxSlider({
		mode: '$transition',
		speed: '$speed',
		pause: '$pause',
		auto: $auto,
		pager: $pager,
		controls: $controls,
		captions: true,
		easing: 'linear'
	});
});
